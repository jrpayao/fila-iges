# Imagem do Streamlit UI — fila-eletiva-ui
# Build context: raiz do projeto. CapRover espera porta 80.
# Streamlit chama API via API_BASE_URL (env), default http://srv-captain--fila-eletiva-api.

FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ui /app/ui
COPY .streamlit /app/.streamlit

# Usuario nao-root
RUN useradd --create-home --uid 1000 appuser && \
    chown -R appuser:appuser /app
USER appuser

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD python -c "import httpx; httpx.get('http://127.0.0.1:80/_stcore/health', timeout=3).raise_for_status()" || exit 1

CMD ["streamlit", "run", "ui/streamlit_app.py", \
     "--server.port=80", \
     "--server.address=0.0.0.0", \
     "--server.headless=true", \
     "--server.enableCORS=false", \
     "--server.enableXsrfProtection=false", \
     "--browser.gatherUsageStats=false"]
